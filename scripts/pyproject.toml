[project]
name = "htg-scripts"
version = "0.1.0"
description = "Testing and comparison tools for htg-service"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "rich>=13.0",
]

[project.scripts]
compare = "compare_elevations:main"
